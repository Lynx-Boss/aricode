// aricode - Challenge 02: Fibonacci
// Compute fib(10) = 55, return as exit code

fn fib(n: i32) -> i32 {
    if (n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main() -> i32 {
    return fib(10);
}
